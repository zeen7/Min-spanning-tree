#include "heapNode.h"
heapNode::heapNode() {
	int node_num = NULL;
	double key = NULL;
	int parent = NULL;
	bool in_pq = NULL; //determines if vertex is still in priorty queue
	int current_index = NULL; //to locate this vertex at constant time
}